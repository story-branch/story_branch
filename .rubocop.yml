inherit_from: .rubocop_todo.yml

Metrics/BlockLength:
  Exclude:
    - 'spec/*.*/*.rb'
AllCops:
  Exclude:
    - '**/*.gemspec'
    - 'exe/*'
    - '**/Gemfile'
    - '.github/**/*'
  NewCops: enable

require:
  - rubocop-rake
  - rubocop-rspec
